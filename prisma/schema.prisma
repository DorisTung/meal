// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  // 若雲端執行期有相容性問題再打開下面這行（通常不用）
  // binaryTargets = ["native", "linux-arm64-openssl-3.0.x", "linux-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("accelerate.prisma-data.net/?api_key=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..y-XSOgbEEyZZN0RwoZ79eV0T4gBq0xcOO-pP0K7V7yQ")
}

model Restaurant {
  id          String   @id @default(cuid())
  name        String
  type        String   // 中式、日式、美式等
  suggestedPeople String  // 建議用餐人數: '1-4', '5-8', '9+'
  phone       String?
  address     String?
  businessDays String  // JSON string: ["monday", "tuesday", ...]
  notSuitableForSummer Boolean @default(false)
  notSuitableForWinter Boolean @default(false)
  notSuitableForRainy  Boolean @default(false)
  distance    Float?   // 距離（公里）
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  history     History[]
}

model History {
  id          String   @id @default(cuid())
  date        DateTime
  restaurantId String
  restaurant  Restaurant @relation(fields: [restaurantId], references: [id])
  peopleCount Int
  weather     String
  isRaining   Boolean
  confirmed   Boolean @default(false)
  createdAt   DateTime @default(now())

  payment     Payment?
}

model Payment {
  id          String   @id @default(cuid())
  historyId   String   @unique
  history     History  @relation(fields: [historyId], references: [id])
  payerName   String?
  amount      Float?
  receiptImage String? // 圖片 URL
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
